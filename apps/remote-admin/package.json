{
  "name": "@fwa/remote-admin",
  "private": true,
  "scripts": {
    "start": "npm run styles --prefix ../.. && webpack serve --config webpack.config.js --mode development",
    "build": "npm run styles --prefix ../.. && webpack --config webpack.config.js --mode production"
  }
}
